spring:
  datasource:
    url: jdbc:tc:postgresql:17:///auth_service_test
    username: test
    password: test

  jpa:
    hibernate:
      ddl-auto: validate

  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: http://localhost:8081/realms/auth-test
